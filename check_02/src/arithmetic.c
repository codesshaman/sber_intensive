#include <stdio.h>

int main()
{
    int x, y, sum, razn, proiz, chastn;
    char z;
    sum = sum(x+y);
    razn = sum(x-y);
    proiz = sum(x*y);

    if (!(scanf("%d%d%c", &x, &y, &z) == 3 && z=='\n'))
    {
        printf("n/a\n");
        
    }

}
